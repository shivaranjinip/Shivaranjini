# AI_ANALYSIS_KEY: Required for secure full-stack analytics advisor logic.
# (If running online in AI Studio, you can set the key secret in the settings menu which serves as a secure fallback.)
AI_ANALYSIS_KEY="your_api_key_here"

# APP_URL: The URL where this applet is hosted.
# Managed and injected automatically at runtime in Cloud envs.
APP_URL="http://localhost:3000"
